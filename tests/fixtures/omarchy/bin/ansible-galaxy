#!/usr/bin/env bash
set -euo pipefail
[[ ${1-} == collection && ${2-} == list && ${3-} == community.general ]] && {
  printf '%s\n' 'community.general 1.0.0'
  exit 0
}
[[ ${1-} == collection && ${2-} == install ]] && exit 0
exit 64
